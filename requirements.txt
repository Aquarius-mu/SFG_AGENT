# 核心依赖（Phase 1 必须）
anthropic>=0.40.0
aiosqlite>=0.20.0
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0
rich>=13.0

# Phase 3 可选：向量记忆（首次安装约 400MB，默认不装）
# 开启方法：uv pip install -r requirements.txt --extra vector
# 或手动：uv pip install sentence-transformers numpy
# sentence-transformers>=3.0
# numpy>=1.24
